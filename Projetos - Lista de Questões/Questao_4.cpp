#include <iostream>
#include <math.h>
#include <stdio.h>
#include <stdlib.h>

using namespace std;

int main()
{
double a , b;

    cout <<"Fale Seu Primeiro Numero: ";
    cin >> a;

    cout <<"Fale Seu Primeiro Numero: ";
    cin >> b;
    
    cout << a <<" / "<< b << " = " << a / b << " e " << b << " / " << a << " = " << b / a;
    
    return 0;
}